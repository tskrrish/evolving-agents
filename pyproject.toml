[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "evolving-agents"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["networkx>=3.0", "matplotlib>=3.5"]

[tool.setuptools.packages.find]
where = ["src"]